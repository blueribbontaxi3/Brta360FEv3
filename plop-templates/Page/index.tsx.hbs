import React from 'react';
import {{pascalCase name}} from 'app/modules/{{pascalCase name}}';

const {{pascalCase name}}Page = (props) => (
  <{{pascalCase name}} {...props} />
);

export default {{pascalCase name}}Page;
