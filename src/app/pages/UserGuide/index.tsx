import React from 'react';
import UserGuide from '../../modules/UserGuide';

const UserGuidePage = (props: any) => {
    return (
        <UserGuide {...props} />
    )
};

export default UserGuidePage;
