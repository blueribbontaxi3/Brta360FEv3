import CreateOrEdit from 'app/modules/Role/CreateOrEdit';
import React from 'react';

const RoleCreateOrEditPage = (props: any) => {
  return (
    <CreateOrEdit {...props} />
  )
};

export default RoleCreateOrEditPage;
