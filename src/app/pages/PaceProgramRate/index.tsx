import PaceProgramRate from '@modules/PaceProgramRate';
import React from 'react';

const PaceProgramRatePage = (props: any) => (
  <PaceProgramRate {...props} />
);

export default PaceProgramRatePage;
