import CreateOrEdit from '@modules/Calculation/CreateOrEdit';
import React from 'react';

const CalculationCreateOrEditPage = (props: any) => {

  return (
    <CreateOrEdit {...props} />
  )

};

export default CalculationCreateOrEditPage;
