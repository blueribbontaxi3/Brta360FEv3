import React from 'react';
import CreateOrEdit from '../../modules/Vendor/CreateOrEdit';

const VendorCreateOrEditPage = (props: any) => {
    return (
        <CreateOrEdit {...props} />
    );
};

export default VendorCreateOrEditPage;
