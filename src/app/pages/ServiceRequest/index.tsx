import React from 'react';
import ServiceRequest from '../../modules/ServiceRequest';

const ServiceRequestPage = (props: any) => (
  <ServiceRequest {...props} />
);

export default ServiceRequestPage;
